
<div class="card">
	<div class="card-header">
		<div class="container ">
			<div class="row pt-2 pb-2 ">
				<div class="col-6">
					<h2 class="text-primary">
						Product List
					</h2>

				</div>
			</div>
		</div>
	</div>

	<div class="card-body pb-0">
		<div class="col-12 text-end pb-3">
			<a asp-controller="Company" asp-action="Upsert" class="btn btn-success">
			<i class="bi bi-plus-circle-fill p-2"></i> Create New Company
		</a>
	</div>



		<table id="tblData" class="table table-bordered table-hover" width="100%">
			<thead>
			<tr class="bg-light">
				<th>
					Name
				</th>
				<th>
					Address
				</th>

				<th>
					City
				</th>

				<th>
					State
				</th>

				<th>
					Phone Number 
				</th>

				<th>
					 
				</th>

				<th>
					 
				</th>
				
				
			</tr>
			</thead>
@* 
			@foreach (var item in Model)
			{
				<tr>
					<td>
						@item.Title
					</td>

					<td>
						@item.ISBN
					</td
					
					><td>
						@item.ListPrice
					</td>

					<td>
						@item.Author
					</td>
					<td>
						@item.Category.Name
					</td>
					    
					<td>
						<a asp-action="Upsert" asp-controller="Product" asp-route-id="@item.Id" class="btn btn-primary">
							<i class="bi bi-pencil me-2"></i> Edit
						</a>
					</td>

					<td>
						<a asp-action="Delete" asp-controller="Product" asp-route-id="@item.Id" onclick="return confirmDelete ('@item.Title');" class="btn btn-danger">
							<i class="bi bi-trash3 me-2"></i> Delete
						</a>
					</td>
				</tr>
			} *@
		</table>
	</div>
</div>


<script>
	function confirmDelete(ItemName) {
		return confirm("Are you sure you want to delete "+ItemName+" product?");
	}
</script>

@section Scripts {
	<script src="~/js/company.js"></script>
}
